🧾 Job Alert Bot — To-Do Task List

🧱 1. Setup & Environment
	•	//Create a new project folder (job-alert-bot)
	•	//Initialize a Git repository (git init)
	•	//Create and activate a virtual environment (python3 -m venv venv)
	•	I//nstall dependencies:
	•	//beautifulsoup4
	•	//requests
	•	//twilio
	•	//python-dotenv (for environment variables)
	•	schedule (for periodic checks)
	•	Create a .env file for Twilio credentials and job filters

    🔍 2. Core Functionality
	•	Implement a function to fetch HTML from GM’s careers page
(requests.get() with error handling)
	•	Parse job listings using BeautifulSoup
	•	Filter results by:
	•	Keywords (e.g., “Software Engineer”)
	•	Location (e.g., “Roanoke, IN”)
	•	Store the last seen job titles/IDs to prevent duplicate alerts
(can use a .json or .txt file)
	•	Implement Twilio SMS function to send alerts

⸻

⏱️ 3. Automation & Scheduling
	•	Add periodic job checks (e.g., every 1 hour) using:
	•	schedule library
or
	•	system cron job (for continuous running)
	•	Include console logging for when a check runs or a new job is found

⸻

⚙️ 4. Configuration & Environment
	•	Move all configurable settings to .env:
	•	Twilio credentials (SID, token, phone numbers)
	•	Search keywords and location
	•	Refresh interval
	•	Load environment variables with dotenv

⸻

🧪 5. Testing
	•	Test scraping function independently
	•	Test Twilio SMS function (using a verified number)
	•	Verify filtering logic correctly matches jobs
	•	Simulate a new job post to confirm notifications work
	•	Handle connection or parsing errors gracefully


📦 6. Project Structure & Docs
	•	Organize files:
    job-alert-bot/
├── job_alert_bot.py
├── requirements.txt
├── .env
├── seen_jobs.json
├── README.md
└── logs/

